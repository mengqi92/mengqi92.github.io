language: node_js
node_js: "10.9"
cache:
  directories:
    - node_modules

env:
  global:
    - GITHUB_REF: github.com/mengqi92/mengqi92.github.io.git

addons:
  apt:
    packages:
      - graphviz

before_install:
  - npm install -g hexo
  - npm install -g hexo-cli
install:
  - npm install
script:
  - hexo cl
  - hexo g --debug -b
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN  # Set in the settings page of your repository, as a secure variable
  keep_history: true
  local_dir: ./public/
  on:
    branch: blog-reloaded
branches:
  only:
    - blog-reloaded
